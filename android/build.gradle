// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {

    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.0'
        classpath 'com.google.gms:google-services:4.4.4'
        // Kotlin mora biti ovdje sa verzijom kako bi subprojekti (Capacitor plugini)
        // mogli primijeniti 'org.jetbrains.kotlin.android' bez konflikta verzija
        // Kotlin verzija se registruje u settings.gradle pluginManagement bloku,
        // NE ovdje — dodavanje ovdje pravi "unknown version" konflikt sa novim plugins DSL-om
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
