diff --git a/android/app/build.gradle b/android/app/build.gradle
index 1b81bd5..d207900 100644
--- a/android/app/build.gradle
+++ b/android/app/build.gradle
@@ -10,8 +10,8 @@ android {
         applicationId "ba.spd.uss.vlake"
         minSdk 24
         targetSdk 34
-        versionCode 401
-        versionName "1.3.1"
+        versionCode 402
+        versionName "1.3.2"
     }
 
     // Koristi ISKLJUČIVO postojeći release ključ; nikad ne generiši zamjenski.
diff --git a/docs/CODEX_CONTEXT.md b/docs/CODEX_CONTEXT.md
index 7cc9118..48c59e3 100644
--- a/docs/CODEX_CONTEXT.md
+++ b/docs/CODEX_CONTEXT.md
@@ -71,6 +71,10 @@ projekat su zasebne cjeline u istom repozitoriju, ne izvršno jezgro tog APK-a.
 - `_poziLoad` razlikuje mrežni neuspjeh, keš i svjež odgovor, filtrira blizinu,
   grupiše detekcije i dopunjava arhivu. Projekcije površine i budućeg rasta
   nisu terenski izmjerena granica niti validiran model ponašanja požara.
+- Od v1.3.2 projekcija požara koristi zaseban `pozariPovrsPane` između
+  referentne karte i markera požara. Klik na pojas otvara detalje incidenta
+  direktno na karti, a dnevni replay kumulativno prikazuje samo tačke i
+  procijenjenu geometriju odabranog požara; ostali požari se ne filtriraju.
 
 ## 2. Pet prioritetnih nalaza
 
diff --git a/index.html b/index.html
index 1681b62..dd56632 100644
--- a/index.html
+++ b/index.html
@@ -5017,7 +5017,7 @@ window.addEventListener('unhandledrejection', function(ev) {
   console.error('Neuhvaćeno odbijeno obećanje:', ev.reason);
   _sigurnosnaMrezaPokusaj();
 });
-const APP_VER = 'v1.3.1';
+const APP_VER = 'v1.3.2';
 // Prikaži verziju na login ekranu (footer je već parsiran iznad ovog scripta)
 { const _authVerEl = document.getElementById('auth-app-ver'); if (_authVerEl) _authVerEl.textContent = APP_VER; }
 
@@ -12481,6 +12481,11 @@ function _vlNagibToggle(on) {
 }
 
 map.createPane('refKarte');    map.getPane('refKarte').style.zIndex    = 620;
+// Interaktivna procjena požara mora biti IZNAD učitane referentne karte
+// (refKarte=620), ali neposredno ISPOD tačaka/markera požara (=645). Ranije je
+// bila u demOverlay=250 pa je referentna karta presretala dodir i popup se nije
+// mogao otvoriti na telefonu.
+map.createPane('pozariPovrsPane'); map.getPane('pozariPovrsPane').style.zIndex = 640;
 // Požari iznad markera (600) — moraju se vidjeti preko svega. DIV markeri, ne
 // canvas: canvas sloj preko cijele karte bi krao klikove svemu ispod (vidi
 // zamku "Canvas pane iznad drugog" u CLAUDE.md); DIV ikonica hvata klik samo
@@ -14613,12 +14618,13 @@ let _poziIncSel = '';
 let _poziIncLayer = null;
 let _poziIncEdit = null; // {mode:'granica'|'tacka', pts:[], tip}
 let _poziAnimCut = null, _poziAnimTimer = null;
+let _poziAnimSel = '', _poziAnimBand = null;
 
 function _poziIncRead() {
   try { const x=JSON.parse(localStorage.getItem(_POZ_INC_KEY)||'{}'); return x && typeof x==='object' ? x : {}; } catch(e) { return {}; }
 }
 function _poziIncWrite(x) { try { localStorage.setItem(_POZ_INC_KEY, JSON.stringify(x)); return true; } catch(e) { return false; } }
-function _poziIncKljuc(g) { return g ? _poziEvtKljuc(g, 200) : ''; }
+function _poziIncKljuc(g) { return g ? (g._animKljuc || _poziEvtKljuc(g, 200)) : ''; }
 function _poziIncGrupa() {
   if (!_poziEvts?.length) return null;
   return _poziEvts.find(g => _poziIncKljuc(g) === _poziIncSel) || _poziEvts[0];
@@ -14735,17 +14741,83 @@ function _poziIncRenderLayer(){
   if(_poziIncEdit?.mode==='granica'&&_poziIncEdit.pts.length)L.polyline(_poziIncEdit.pts.map(p=>[p.la,p.lo]),{pane:'pozariPane',color:'#22c55e',weight:3,dashArray:'5,4'}).addTo(_poziIncLayer);
   _poziIncLayer.addTo(map);
 }
-function _poziAnimRange(){const ts=(_poziPts||[]).map(p=>Date.parse(p.dt)).filter(isFinite).sort((a,b)=>a-b);return ts.length?[ts[0],ts[ts.length-1]]:null;}
+function _poziAnimGrupa(){
+  const evts=_poziEvts||[];
+  if(!_poziAnimSel)return _poziIncGrupa()||evts[0]||null;
+  return evts.find(g=>_poziIncKljuc(g)===_poziAnimSel)||null;
+}
+function _poziAnimRange(){
+  const g=_poziAnimGrupa(),izvor=g?.pts||_poziPts||[];
+  const ts=izvor.map(p=>Date.parse(p.dt)).filter(isFinite).sort((a,b)=>a-b);
+  return ts.length?[ts[0],ts[ts.length-1]]:null;
+}
+// Jedan korak = jedan kalendarski dan u kojem je satelit vidio odabrani požar.
+// Cut je zadnja stvarna detekcija tog dana, pa animacija ne izmišlja kretanje
+// između dva preleta nego prikazuje kumulativno ono što je stvarno zabilježeno.
+function _poziAnimKoraci(g){
+  const poDanu=new Map();
+  (g?.pts||[]).forEach(p=>{const t=Date.parse(p.dt);if(!isFinite(t))return;const dan=new Date(t).toISOString().slice(0,10);const x=poDanu.get(dan)||{dan,cut:t,broj:0};x.cut=Math.max(x.cut,t);x.broj++;poDanu.set(dan,x);});
+  return [...poDanu.values()].sort((a,b)=>a.cut-b.cut);
+}
 function _poziAnimGrupe(){
   if(!isFinite(_poziAnimCut))return _poziEvts||[];
+  if(_poziAnimSel){
+    return (_poziEvts||[]).map(g=>{
+      if(_poziIncKljuc(g)!==_poziAnimSel)return g;
+      const pts=(g.pts||[]).filter(p=>(Date.parse(p.dt)||0)<=_poziAnimCut);
+      if(!pts.length)return null;
+      // Već potvrđene tačke pripadaju istom odabranom incidentu. Ogroman prag
+      // ovdje samo čuva grupu na okupu tokom replaya; ne utiče na normalno
+      // grupisanje zasebnih požara.
+      const a=_poziGrupisi(pts,Number.MAX_SAFE_INTEGER)[0];
+      return a?{...a,d:g.d,nov:false,_animKljuc:_poziAnimSel}:null;
+    }).filter(Boolean);
+  }
   const pts=(_poziPts||[]).filter(p=>(Date.parse(p.dt)||0)<=_poziAnimCut),ref=_poziRefTacka();
   return _poziGrupeBlizu(_poziGrupisi(pts),ref).map(g=>({...g,nov:false}));
 }
-function _poziAnimSet(v){const r=_poziAnimRange();if(!r)return;_poziAnimCut=r[0]+(r[1]-r[0])*(Number(v)/100);_poziRender();const el=document.getElementById('pozi-anim-datum');if(el)el.textContent=new Date(_poziAnimCut).toLocaleString('bs-BA');}
+function _poziAnimSet(v){const r=_poziAnimRange();if(!r)return;if(!_poziAnimSel){const g=_poziIncGrupa();if(g)_poziAnimSel=_poziIncKljuc(g);}_poziAnimCut=r[0]+(r[1]-r[0])*(Number(v)/100);_poziRender();_poziMapHudRender();const el=document.getElementById('pozi-anim-datum');if(el)el.textContent=new Date(_poziAnimCut).toLocaleString('bs-BA');}
+function _poziAnimStop(){if(_poziAnimTimer){clearInterval(_poziAnimTimer);_poziAnimTimer=null;}}
+function _poziAnimSetKorak(i){
+  const g=_poziAnimGrupa(),k=_poziAnimKoraci(g);if(!k.length)return;
+  const n=Math.max(0,Math.min(k.length-1,Number(i)||0));_poziAnimCut=k[n].cut;
+  _poziRender();_poziMapHudRender();
+}
 function _poziAnimPlay(){
-  if(_poziAnimTimer){clearInterval(_poziAnimTimer);_poziAnimTimer=null;return;}
-  let s=document.getElementById('pozi-anim');if(!s)return;if(Number(s.value)>=100)s.value=0;
-  _poziAnimTimer=setInterval(()=>{s=document.getElementById('pozi-anim');if(!s){clearInterval(_poziAnimTimer);_poziAnimTimer=null;return;}s.value=Math.min(100,Number(s.value)+4);_poziAnimSet(s.value);if(Number(s.value)>=100){clearInterval(_poziAnimTimer);_poziAnimTimer=null;}},350);
+  if(_poziAnimTimer){_poziAnimStop();_poziMapHudRender();return;}
+  const g=_poziAnimGrupa();if(!g)return;if(!_poziAnimSel)_poziAnimSel=_poziIncKljuc(g);
+  const k=_poziAnimKoraci(g);if(!k.length)return;
+  let i=k.findIndex(x=>x.cut>=_poziAnimCut);if(!isFinite(_poziAnimCut)||i<0||i>=k.length-1)i=-1;
+  _poziAnimSetKorak(i+1);
+  _poziAnimTimer=setInterval(()=>{const gg=_poziAnimGrupa(),kk=_poziAnimKoraci(gg);let j=kk.findIndex(x=>x.cut>=_poziAnimCut);if(j<0)j=0;if(j>=kk.length-1){_poziAnimStop();_poziMapHudRender();return;}_poziAnimSetKorak(j+1);},850);
+  _poziMapHudRender();
+}
+
+function _poziMapHudClose(){
+  _poziAnimStop();_poziAnimCut=null;_poziAnimSel='';_poziAnimBand=null;
+  document.getElementById('pozi-map-hud')?.remove();_poziRender();
+}
+function _poziMapOtvori(g,tr){
+  _poziAnimStop();_poziAnimSel=_poziIncKljuc(g);_poziIncSel=_poziAnimSel;
+  _poziAnimCut=null;_poziAnimBand=tr?{id:tr.id,naziv:tr.naziv,ha:tr.ha,od:tr.od,do:tr.do,broj:tr.broj}:null;
+  _poziIncRenderLayer();_poziMapHudRender();
+}
+function _poziMapHudRender(){
+  let el=document.getElementById('pozi-map-hud');const g=_poziAnimGrupa();
+  if(!_poziAnimSel||!g){if(el)el.remove();return;}
+  if(!el){el=document.createElement('div');el.id='pozi-map-hud';map.getContainer().appendChild(el);}
+  el.style.cssText='position:absolute;z-index:1000;left:10px;right:10px;bottom:82px;max-height:46%;overflow:auto;background:#0f172af2;border:1px solid #f97316;border-radius:13px;padding:10px 12px;color:#e2e8f0;box-shadow:0 8px 28px #0009;box-sizing:border-box';
+  const k=_poziAnimKoraci(g),zadnji=k.length-1,idx=isFinite(_poziAnimCut)?Math.max(0,k.findIndex(x=>x.cut>=_poziAnimCut)):zadnji;
+  const prikaz=(_poziAnimGrupe()||[]).find(x=>x._animKljuc===_poziAnimSel)||g;
+  const prSve=_poziProj(g),prSad=_poziProj(prikaz),pou=_poziPovrsPouzdanost(g,prSve);
+  const ref=_poziRefTacka(),udalj=dst(ref.la,ref.lo,g.la,g.lo),smjer=_azimutSmjer(_bearing(ref.la,ref.lo,g.la,g.lo)),gk=wgsToMGI(g.la,g.lo);
+  const fmt=t=>isFinite(t)?new Date(t).toLocaleString('bs-BA'):'—';
+  const red=(l,v)=>'<div style="display:flex;justify-content:space-between;gap:10px;padding:2px 0;font-size:10.5px"><span style="color:#94a3b8">'+l+'</span><b style="text-align:right;color:#e2e8f0">'+v+'</b></div>';
+  const band=_poziAnimBand?'<div style="margin:6px 0;padding:6px 8px;border-radius:8px;background:#7c2d123d;border:1px solid #9a3412;font-size:10.5px;color:#fdba74">Kliknuti pojas: <b>'+_escHtml(_poziAnimBand.naziv)+'</b> · ≈ '+_poziPovrsTxt(_poziAnimBand.ha)+'<br><span style="color:#cbd5e1">'+_escHtml(_poziPeriodPojasa(_poziAnimBand.od,_poziAnimBand.do))+'</span></div>':'';
+  const korak=k[idx]||null,detSad=(prikaz?.pts||[]).length;
+  el.innerHTML='<div style="display:flex;align-items:center;gap:8px"><div style="font-size:13px;font-weight:800;color:#fb923c;flex:1">🔥 Razvoj odabranog požara</div><button onclick="_poziMapHudClose()" style="border:0;background:#334155;color:white;border-radius:50%;width:27px;height:27px;font-size:18px">×</button></div>'+band+
+    red('Stanje',_poziStatusPozara(g))+red('Početak požara',fmt(g.prvi))+red('Zadnje viđenje',fmt(g.zadnji))+red('Trajanje',_poziTrajanje(g.prvi,g.zadnji))+red('Ukupna procjena',_poziPovrsTxt(prSve?.haUkupno))+red('Detekcije',g.broj+' · '+_escHtml((g.sateliti||[]).join(', ')||'—'))+red('FRP max',g.frpMax?g.frpMax.toFixed(1)+' MW':'—')+red('Raspon detekcija',g.spanM>0?fmtL(g.spanM):'—')+red('Udaljenost',fmtL(udalj)+' '+smjer+(ref.gps?'':' (od centra karte)'))+red('Pouzdanost','<span style="color:'+pou.boja+'">'+pou.txt+'</span>')+(gk?'<div style="font-size:10px;color:#fbbf24;font-family:monospace;margin-top:4px">Y: '+gk.y+' · X: '+gk.x+'</div>':'')+
+    '<div style="margin-top:7px;padding-top:7px;border-top:1px solid #334155"><div style="font-size:10.5px;color:#7dd3fc;margin-bottom:5px">'+(korak?('Dan '+(idx+1)+'/'+k.length+' · '+new Date(korak.cut).toLocaleDateString('bs-BA')+' · '+detSad+'/'+g.broj+' detekcija · ≈ '+_poziPovrsTxt(prSad?.haUkupno)):'Nema vremenskih koraka')+'</div><div style="display:flex;gap:7px;align-items:center"><button onclick="_poziAnimPlay()" style="padding:7px 10px;border:1px solid #f97316;border-radius:8px;background:#7c2d12;color:white;font-weight:700">'+(_poziAnimTimer?'⏸ Pauza':'▶ Play')+'</button><input type="range" min="0" max="'+Math.max(0,zadnji)+'" value="'+Math.max(0,idx)+'" oninput="_poziAnimSetKorak(this.value)" style="flex:1;accent-color:#f97316" '+(k.length<2?'disabled':'')+'></div><div style="font-size:9px;color:#64748b;margin-top:4px">Karta kumulativno prikazuje tačke i procijenjenu površinu do izabranog dana.</div></div>';
 }
 function _poziIncIzvjestaj(){
   const g=_poziIncGrupa(),inc=_poziIncGet(g,true);if(!g||!inc)return;const pr=_poziProj(g),odj=_poziOdjeli(g,inc),pou=_poziPovrsPouzdanost(g,pr),fotos=_poziIncFotos(g,inc),skica=_poziIncSvg(g,inc);
@@ -14793,7 +14865,7 @@ function _poziOpozAzuriraj() {
     if (typeof _demLegendUpdate === 'function') _demLegendUpdate();
     return;
   }
-  const grp = L.layerGroup([], { pane: 'demOverlay' });
+  const grp = L.layerGroup([], { pane: 'pozariPovrsPane' });
   let nacrtano = 0;
   (typeof _poziAnimGrupe === 'function' ? _poziAnimGrupe() : (_poziEvts || [])).forEach(g => {
     const pr = _poziProj(g);
@@ -14809,13 +14881,13 @@ function _poziOpozAzuriraj() {
     // mapa i učitani KML/SHP slojevi nastavljaju raditi kao ranije.
     // Obrnut redoslijed: najstarija traka prva (dolje), aktivan front zadnji (gore).
     pr.trake.slice().reverse().forEach(tr => {
-      L.geoJSON(tr.geom, { pane:'demOverlay', interactive:true,
+      L.geoJSON(tr.geom, { pane:'pozariPovrsPane', interactive:true,
         style:{ color:tr.linija, weight:1, fillColor:tr.fill, fillOpacity:tr.op },
-        onEachFeature:(f,l)=>l.bindPopup(_poziPojasPopupHtml(g,tr,pr),{maxWidth:290,className:'kml-info-popup'}) }).addTo(grp);
+        onEachFeature:(f,l)=>{l.bindPopup(_poziPojasPopupHtml(g,tr,pr),{maxWidth:290,className:'kml-info-popup'});l.on('click',()=>_poziMapOtvori(g,tr));} }).addTo(grp);
     });
     // Vanjska isprekidana kontura ukupnog zahvata — bez nje se, kad su trake
     // slične boje, ne vidi dokle požar UKUPNO seže.
-    L.geoJSON(pr.ukupno, { pane:'demOverlay', interactive:false,
+    L.geoJSON(pr.ukupno, { pane:'pozariPovrsPane', interactive:false,
       style:{ color:'#fdba74', weight:2, dashArray:'6,4', fill:false, opacity:0.9 } }).addTo(grp);
     nacrtano++;
   });
diff --git a/sw.js b/sw.js
index c0145ae..4423ec6 100644
--- a/sw.js
+++ b/sw.js
@@ -2,7 +2,7 @@
 // Service Worker — ŠPD Unsko-sanske šume
 // Promijeni APP_VERSION pri svakom deploymentu → okida update
 // =====================================================================
-const APP_VERSION = '1.3.1';
+const APP_VERSION = '1.3.2';
 const APP_CACHE   = 'tvlake-app-v' + APP_VERSION;
 const TILE_CACHE  = 'tvlake-tiles-v1';
 const LIB_CACHE   = 'tvlake-lib-v1';
diff --git a/tests/js/pozari-incident.test.js b/tests/js/pozari-incident.test.js
index 790ed34..2ebae41 100644
--- a/tests/js/pozari-incident.test.js
+++ b/tests/js/pozari-incident.test.js
@@ -2,8 +2,8 @@
 const fs=require('fs'),path=require('path'),assert=require('assert');
 const html=fs.readFileSync(path.join(__dirname,'../../index.html'),'utf8');
 function fn(n){const s=html.indexOf('function '+n+'(');assert.ok(s>=0,'nedostaje '+n);let i=html.indexOf('{',s),d=0;for(;i<html.length;i++){if(html[i]==='{')d++;else if(html[i]==='}'&&!--d)return html.slice(s,i+1);}throw Error('nezatvorena '+n);}
-const src=['_poziOrijent','_poziSegmentiSijeku','_poziVrstaPrepreke','_poziPragIzmedju','_poziTrajanje','_poziPovrsPouzdanost'].map(fn).join('\n');
-const api=new Function(src+';return {_poziSegmentiSijeku,_poziVrstaPrepreke,_poziPragIzmedju,_poziTrajanje,_poziPovrsPouzdanost}')();
+const src=['_poziOrijent','_poziSegmentiSijeku','_poziVrstaPrepreke','_poziPragIzmedju','_poziTrajanje','_poziPovrsPouzdanost','_poziAnimKoraci'].map(fn).join('\n');
+const api=new Function(src+';return {_poziSegmentiSijeku,_poziVrstaPrepreke,_poziPragIzmedju,_poziTrajanje,_poziPovrsPouzdanost,_poziAnimKoraci}')();
 let ok=0;function t(n,f){try{f();console.log('  ✔ '+n);ok++;}catch(e){console.error('  ✘ '+n+'\n    '+e.message);process.exitCode=1;}}
 console.log('Požari v1.3 — prepreke i incident:');
 t('rijeka je tvrda prepreka',()=>assert.equal(api._poziVrstaPrepreke('Rijeka Una'),'voda'));
@@ -16,6 +16,9 @@ t('površina sa više senzora i preleta ima visoku pouzdanost',()=>{const pts=Ar
 t('UI razdvaja satelit, EFFIS i teren',()=>['Satelitska procjena','EFFIS (zaseban sloj)','Terenska granica'].forEach(x=>assert.ok(html.includes(x))));
 t('UI ima animaciju, odjele, operativne tačke i PDF',()=>['Razvoj požara kroz vrijeme','Operativne tačke','Zahvaćeni odjeli','PDF izvještaj / štampa'].forEach(x=>assert.ok(html.includes(x))));
 t('tekuća godina se zakazuje u pozadini',()=>assert.ok(html.includes('function _povGodLoadPozadina(')));
-t('pojas projekcije je klikabilan i popup prikazuje hektare',()=>{assert.ok(html.includes('function _poziPojasPopupHtml('));assert.ok(/interactive:true/.test(html));});
+t('pojas projekcije je iznad karte i otvara detalje incidenta',()=>{assert.ok(html.includes("map.createPane('pozariPovrsPane')"));assert.ok(html.includes("pane:'pozariPovrsPane', interactive:true"));assert.ok(html.includes("l.on('click',()=>_poziMapOtvori(g,tr))"));});
+t('detalji i Play se prikazuju direktno na karti',()=>['pozi-map-hud','Početak požara','Ukupna procjena','▶ Play','Karta kumulativno prikazuje tačke'].forEach(x=>assert.ok(html.includes(x))));
+t('vremenska crta grupiše detekcije po danima',()=>{const k=api._poziAnimKoraci({pts:[{dt:'2026-09-10T08:00:00Z'},{dt:'2026-09-10T18:00:00Z'},{dt:'2026-09-12T09:00:00Z'}]});assert.equal(k.length,2);assert.equal(k[0].broj,2);assert.equal(k[0].cut,Date.parse('2026-09-10T18:00:00Z'));});
+t('replay filtrira samo odabrani incident, ne sve požare',()=>{const a={id:'A',pts:[{dt:'2026-09-10T08:00:00Z'},{dt:'2026-09-12T08:00:00Z'}],d:1},b={id:'B',pts:[{dt:'2026-09-12T08:00:00Z'}],d:2};const run=new Function('_poziAnimCut','_poziAnimSel','_poziEvts','_poziIncKljuc','_poziGrupisi','_poziPts','_poziRefTacka','_poziGrupeBlizu',fn('_poziAnimGrupe')+';return _poziAnimGrupe()');const out=run(Date.parse('2026-09-10T23:00:00Z'),'A',[a,b],g=>g._animKljuc||g.id,pts=>[{id:'A',pts,la:1,lo:1}],[],()=>({}),x=>x);assert.equal(out.length,2);assert.equal(out[0].pts.length,1);assert.equal(out[1],b);assert.equal(out[0]._animKljuc,'A');});
 t('pojedinačni požar koristi manji marker',()=>assert.ok(html.includes('poz-mk-pojedinac')));
 console.log('\n'+ok+' prošlo, '+(process.exitCode?1:0)+' palo');
